apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-pvc
  namespace: monitoring
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: cephfs-hdd
  resources:
    requests:
      storage: 2Gi
